server:
  port: 7001

eureka:
  instance:
    hostname: localhost

  client:
    register-with-eureka: false
    fetchRegistry: false #抓取注册信息
    service-url:
      defaultZone: http://localhost:7001/eureka
